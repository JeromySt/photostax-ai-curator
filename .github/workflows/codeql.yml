name: "CodeQL"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * 1'  # Run every Monday at midnight

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '10.0.x'

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: csharp
        queries: security-extended,security-and-quality

    - name: Build non-MAUI projects
      run: |
        dotnet build src/PhotostaxAiCurator.Domain/PhotostaxAiCurator.Domain.csproj --configuration Release
        dotnet build src/PhotostaxAiCurator.Services/PhotostaxAiCurator.Services.csproj --configuration Release
        dotnet build src/PhotostaxAiCurator.Infrastructure/PhotostaxAiCurator.Infrastructure.csproj --configuration Release

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
