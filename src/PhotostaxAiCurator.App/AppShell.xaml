<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PhotostaxAiCurator.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:PhotostaxAiCurator.App.Views"
    Title="PhotoStax AI Curator"
    Shell.FlyoutBehavior="Flyout">

    <FlyoutItem Title="Scan" Icon="{OnPlatform Default=''}">
        <ShellContent
            Title="Scan Photos"
            ContentTemplate="{DataTemplate views:ScanPage}"
            Route="ScanPage" />
    </FlyoutItem>

    <FlyoutItem Title="Dashboard" Icon="{OnPlatform Default=''}">
        <ShellContent
            Title="Dashboard"
            ContentTemplate="{DataTemplate views:DashboardPage}"
            Route="DashboardPage" />
    </FlyoutItem>

    <FlyoutItem Title="Review" Icon="{OnPlatform Default=''}">
        <ShellContent
            Title="Review Queue"
            ContentTemplate="{DataTemplate views:ReviewPage}"
            Route="ReviewPage" />
    </FlyoutItem>

    <FlyoutItem Title="Settings" Icon="{OnPlatform Default=''}">
        <ShellContent
            Title="Settings"
            ContentTemplate="{DataTemplate views:SettingsPage}"
            Route="SettingsPage" />
    </FlyoutItem>

</Shell>
